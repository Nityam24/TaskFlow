{
  "name": "task-management-system",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm --prefix frontend run build"
  },
  "engines": {
    "node": "24.x"
  }
}
