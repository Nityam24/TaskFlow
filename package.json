{
  "name": "task-management-system",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm --prefix frontend run build"
  },
  "engines": {
    "node": "20.x"
  }
}
